export default class TransformGizmos {
    constructor() {
        this.gizmo_objects = [];
    }

    addGizmoObjects(gizmo_objects) {
        this.gizmo_objects = gizmo_objects;
    }

    // pass in the mesh and the vao
    // create the SceneObject and AABB.
}